### 2026-06-27

- 完成 V3 非事件驱动策略复跑与治理文档补齐。
- 执行命令：`python scripts/run_v3_phase0_backtest.py --strategies B C E K H --skip-funding-download`。
- 当前复跑结果：
  - `B / funding_extreme` → `ABORT`，`n=238`，`EV=-0.1434R`，`PF=0.6131`
  - `C / beta_decouple` → `ABORT`，`n=23`，`EV=-0.1523R`，`PF=0.3454`
  - `E / pre_funding_unwind` → `ABORT`，`n=60`，`EV=-0.1780R`，`PF=0.4628`
  - `K / pair_mr` → `ABORT`，`n=2091`，`EV=-0.3035R`，`PF=0.4120`
  - `H / oi_velocity` → `ABORT`，`0` 笔交易
- 关键结论：`reports/v3_output/v3_phase0_combo.md` 已更新为 `❌ 禁止进入` ensemble；`C(beta_decouple)` 从旧报告 `PASS / EV=0.6108R` 翻转为当前 `ABORT / EV=-0.1523R`，证明旧研究结论不可继续沿用。
- 新增文档：
  - `docs/reports/2026-06-27-non-event-driven-strategy-rerun-report.md`
  - `docs/strategies/00-strategy-template.md`
  - `docs/strategies/00-strategy-review-checklist.md`
- 更新文档：`docs/plans/2026-06-27-research-live-promotion-parity-root-fix.md` 补入复跑基线与 proof command。
- 运行告警：`scripts/run_v3_phase0_backtest.py:422` 出现 pandas `pct_change` 的 `fill_method` 弃用告警；本次仅记录，不扩大修复范围。

- 修复 `opencode` 调用 `openai-proxy` 模型时缓存命中率偏低的主配置问题。
- 配置变更：
  - `opencode.json`：将 `gpt-5.5`、`gpt-5.4`、`gpt-5.4-mini` 的 `store` 从 `false` 改为 `true`。
  - `C:\Users\Rusz\.config\opencode\opencode.jsonc`：同步将同名 3 个模型的 `store` 从 `false` 改为 `true`，避免仓库级与用户级配置不一致。
- 根因结论：命中率偏低的首要原因是 `openai-proxy` 模型显式禁用了请求存储；`runtime_fallback` 与多 provider 回退仍会带来额外 prompt churn，但本次未扩大修改范围。
- 验证命令：`claude-tap --version`、`claude-tap --tap-client opencode --tap-no-launch --tap-no-open --tap-no-live --help`、`opencode --version`、`opencode models openai-proxy`、配置解析脚本均通过。

### 2026-06-27

- 完成 live-echo 漂移分析需求与策略计划文档拆分任务。
- 新建/修改文档：
  - `docs/requirements/2026-06-27-live-echo-drift-requirements.md`：需求 9 节骨架完整，通过实施就绪门禁，227 行。
  - `docs/reports/2026-06-27-live-echo-drift-analysis-report.md`：基于 `data/live_echo/trades.csv`（16 条记录）与 `state.json` 的 6 处漂移分析，44 处证据引用，237 行。
  - `docs/design/2026-06-27-strategy-v3-edge-design.md`：从原 V3.1 计划拆分出的 edge 信号设计，358 行。
  - `docs/architecture/2026-06-27-strategy-v3-execution-architecture.md`：从原 V3.1 计划拆分出的共享执行架构，420 行。
  - `docs/plans/2026-06-25-1500-strategy-v3-zero-data-critical-strike.md`：从 1241 行修剪到 334 行，保留执行视角。
  - `docs/plans/2026-06-27-live-echo-drift-requirement-and-doc-split.md`：本任务执行计划已结案，5 项 closure gates 全部勾选。
- 5 个文档之间补充了 21 处 markdown 交叉链接。
- 验证命令：行数统计、需求 10 个章节标题、报告 44 处证据引用、交叉链接 grep 均通过。
- 未修改实盘/纸交易/策略代码，未重启实盘。
- 后续如需重启实盘，将另立 `docs/plans/xxxx-live-echo-restart.md`。

- 完成 V3 `weekend_wick` 回测/实盘偏差根因修复：
  - 根因定位：`scripts/run_v3_phase0_backtest.py` 原先对所有策略都把 `1m` klines 传给 `simulate_exits()`；但 `weekend_wick` 的 `bar_freq="1h"` 且 `time_stop_bars=2`，因此回测实际只持仓约 2 分钟，而不是 2 小时。
  - 代码修复：`run_strategy_on_universe()` 改为仅对 `is_event_driven=False` 的策略按 `strategy.config.bar_freq` resample exit klines；`listing_fade` 仍保持 `1m` exit 路径不变。
  - 新增回归：`src/backtest/tests/test_v3_phase0_exit_alignment.py`，锁定「非 event-driven 策略必须按自身 bar 频率做 exit 模拟，event-driven 仍走分钟级」这一约束。
- 回放验证结果：
  - `python -m pytest src/backtest/tests/test_v3_phase0_exit_alignment.py -q` → `2 passed`
  - `python -m pytest src/backtest/tests/test_live_echo_sltp.py src/backtest/tests/test_paper_pipeline_strategy_edges.py src/backtest/tests/test_weekend_wick_strategy.py -q` → `11 passed`
  - `python scripts/run_v3_phase0_backtest.py --strategies D` → `weekend_wick` 从原 `PASS / EV=1.2113R / PF=56.2137` 修正为 `ABORT / EV=0.0441R / PF=1.0934`
  - `reports/v3_output/per_strategy/weekend_wick/summary.json` 现为 `avg_holding_bars=1.65`；`reports/v3_output/per_strategy/weekend_wick/trades.csv` 中持仓已从分钟级变为 1~2 小时级，证明假收益主要来自 exit 频率错配。
