# Skill 索引

将此目录用于可复用的提示词和工作流程手册。

这些不是一次性的聊天消息，而是可复用的仓库记忆。

技能应主要捕获可复用的工作方法、审查方法或审计方法。不要将技能用作需求真相、设计真相或架构真相的替代品。

技能库不是吸引子。如果不通过 `AGENTS.md`、`docs/index.md`、活跃需求和所有者文档进行路由，大型技能库通常会退化为结构化的氛围编码。

这些提示词是复制项目的通用默认值。复制模板后，您必须根据项目的真实所有者文档、受保护区域、验证栈、命名约定、已知故障模式和误报容忍度对其进行定制。

## Skill 路由规则

选择技能前：

1. 首先阅读相关需求文档和所有者文档。
2. 使用 `AGENTS.md` 对任务类型进行分类。
3. 根据工作方法而非仅业务标签来选择技能。
4. 如果多个技能都适用，请在实施前请独立的子代理或审查员进行选择。
5. 如果没有现有技能明确适用，记录 `Skill: none` 并按正常的文档驱动流程继续。
6. 对于非琐碎的方案，在方案中记录技能选择依据和审查结果。

不要将广泛的业务场景技能作为项目特定所有者文档的替代品。如果某个场景频繁重复，首先检查是否缺少路由、所有者文档或方案指导。只有当可复用的工作方法稳定后，才将其提升为技能。

## Skill Registry

- **重要**，默认不自动加载 skills。以下方表格规格进行加载。
- 当需要编写 plan 的时候，先尝试调用 `.agents\skills\writing-plans\SKILL.md` 。但该skill的默认模板、存放位置等不是本项目需要的。写计划文档的一切规范以`docs\plans\00-plan-authoring-and-execution-guide.md`为准。


| 技能 | 适用场景 | 不适用场景 | 所需输入 | 预期输出 |
| ---- | -------- | ---------- | -------- | -------- |
| `brainstorming` | 任何创造性工作之前——创建功能、构建组件、添加功能或修改行为 | 机械执行、无设计空间的小修小补 | 用户需求、当前代码上下文、设计约束 | 经过分析的设计方案、需求澄清、书面规格说明 |
| `chinese-code-review` | 收到代码审查反馈后需要处理，尤其反馈不明确或技术上有疑问；团队使用中文沟通 | 未收到代码审查反馈；非中文沟通团队 | 审查反馈内容、相关代码、归属文档 | 对反馈的响应、修改实施或技术反驳 |
| `chinese-commit-conventions` | 需要规范中文 commit message；国内团队适配 Conventional Commits 中文模板 | 非中文项目；无需规范提交信息 | 项目环境配置、提交历史、变更内容 | 符合规范的 commit message 格式、lint 配置 |
| `chinese-documentation` | 编写或优化中文技术文档、README；需要统一中文排版指北 | 纯英文文档编写 | 待编写的文档内容、技术规范 | 格式规范、可读性强的中文文档 |
| `chinese-git-workflow` | 使用国内 Git 平台（Gitee、Coding.net、极狐 GitLab、CNB）；适配国内团队习惯 | 国际项目；不涉及国内平台适配 | git 配置、平台信息、工作流规则 | 符合规范的 Git 工作流操作、CI 配置 |
| `dispatching-parallel-agents` | 面对 2 个以上可以独立进行、无共享状态或顺序依赖的任务 | 任务高度耦合、共享状态或需强顺序执行 | 任务列表、问题描述、各任务上下文 | 独立任务的并行处理结果 |
| `executing-plans` | 有一份书面实现计划需要在单独会话中执行，并设有审查检查点 | 无书面计划、需要实时探索的开放任务 | 计划文件、任务清单、代码库上下文 | 按计划执行的任务完成报告、验证结果 |
| `finishing-a-development-branch` | 实现完成、所有测试通过、需要决定如何集成工作（合并、PR、清理） | 工作仍在进行中、测试未通过 | 测试验证结果、分支信息、工作区状态 | 分支集成决策（合并/PR/丢弃）及执行 |
| `receiving-code-review` | 收到代码审查反馈后、实施建议之前，尤其反馈不明确或技术上有疑问 | 未收到审查反馈；反馈已明确且无需验证 | 审查反馈内容、相关代码、归属文档 | 对审查意见的回应、实施修改或技术反驳 |
| `requesting-code-review` | 完成任务、实现重要功能或合并前，请求验证工作成果 | 无需审查的琐碎修改；工作尚未完成 | 完成的代码变更、相关文档、测试证据 | 审查结果、后续修改建议 |
| `subagent-driven-development` | 在当前会话中执行包含独立任务的实现计划 | 任务高度耦合、需要强顺序依赖或大量上下文共享 | 实现计划、代码库上下文 | 每个任务的完成结果、审查和整合 |
| `systematic-debugging` | 遇到任何 bug、测试失败或异常行为时 | 仅凭猜测修复问题；问题已明显且局部 | 问题现象、复现步骤、日志、代码上下文 | 根本原因分析、验证修复 |
| `test-driven-development` | 实现任何功能或修复 bug 之前 | 一次性原型、生成代码等不需要长期维护的场景 | 功能需求或问题描述 | 测试驱动下的代码实现与验证 |
| `using-git-worktrees` | 需要开始与当前工作区隔离的功能开发；执行实现计划前确保隔离工作区 | 无需隔离的微小修改 | 工作区状态、项目设置 | 隔离的工作区、初始环境配置、基线测试验证 |
| `verification-before-completion` | 宣称工作完成、已修复或测试通过之前；提交或创建 PR 之前 | 工作明显未完成 | 测试结果、修复内容、变更 diff | 验证通过或失败的结果报告、证据支撑 |
| `workflow-runner` | 用户提供 .yaml 工作流文件或要求多角色协作完成任务时 | 无 YAML 工作流定义的任务 | agency-orchestrator YAML 工作流定义 | 按定义流程执行的输出结果 |
| `writing-plans` | 有规格说明或需求用于多步骤任务时；动手写代码之前 | 无规格说明的琐碎单文件修改 | 需求文档、代码库上下文、设计约束 | 全面的实现计划（含任务分解、文件路径、测试步骤） |

## Starter Skills

- `.agents\skills` 内的skills。

## 与工具原生技能的关系

`docs/skills/` 存放的是方法和审计类型的技能，它们位于仓库内部，可在不同工具和编辑器之间保持可移植性。一些 AI 工具也支持加载其原生的技能（例如工具自动加载的项目本地技能目录）。两者是互补关系，而非竞争关系：

- 将可复用的审查/审计方法和提示词放在 `docs/skills/` 中，以便与文档一起进行版本控制，并可供任何代理或人工阅读
- 如果你使用特定工具，可将工具加载的操作型技能（框架特定的操作指南、代码生成配方、调试配方）放在该工具自身的约定目录中
- 无论技能实际存放在何处，都应通过 `AGENTS.md`、`docs/index.md` 和所有者文档进行路由；技能选择的是工作方法，不能替代所有者文档的路由功能

本模板本身保持工具中立，不假定使用任何特定的 AI 工具。
